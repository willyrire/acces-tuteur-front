// src/api/auth.js
import API from "@/api/client";

async function createAccountRequest(email, password, firstName, lastName, role, city, location, phone)
{

    // On commence par vérifier le courriel.
}
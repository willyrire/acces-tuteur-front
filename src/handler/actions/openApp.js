import { fastRedirect } from "@/utils/tools/fastRedirect";

const APP_DASHBOARD_URL = "http://localhost:5173/dashboard";

const openApp = async () => {
    // Créer une micro session pour l'application

}

export default openApp;